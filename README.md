# riverbraid-ssg
**Static Site Substrate**

A deterministic documentation engine for the Riverbraid Accords. It ensures that architectural specs and verification logs are rendered from a stationary, tamper-evident source.

### **Verification**
`node verify-docs.js`
> Validates that the rendered output matches the verified filesystem hash.
